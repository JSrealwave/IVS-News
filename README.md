# IVS News Pipeline

Automated technical news aggregator for Intelligent Video Surveillance.

## Quick Start
1. Add your `XAI_API_KEY` and `TAVILY_API_KEY` to `.env`
2. `uv sync` or `pip install -r requirements.txt`
3. `python pipeline.py --run`

First run will create `articles.json` with curated results.
